# URL base da API do backend (tcl_backend), incluindo o prefixo /api.
# Em dispositivo físico/emulador NÃO uses "localhost" — usa o IP da máquina na rede local (LAN).
# Ex.: ipconfig (Windows) -> "Endereço IPv4" do adaptador Wi-Fi/Ethernet.
EXPO_PUBLIC_API_URL=http://<o-teu-ip-lan>:3000/api
